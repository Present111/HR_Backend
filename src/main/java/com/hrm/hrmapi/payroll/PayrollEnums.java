package com.hrm.hrmapi.payroll;

public class PayrollEnums {
    public enum CycleStatus { DRAFT, LOCKED, PAID }
    public enum ComponentKind { EARNING, DEDUCTION }
    public enum CalcType { FIXED, FORMULA }
}
